[General]
proxy-test-url = https://health.example.invalid/ping
always-real-ip = api.example.invalid, *.real-ip.example.invalid

[Proxy]
DNS Fixture Proxy = http, dns-proxy.example.invalid, 8080

[Proxy Group]
DNS Auto = url-test, DNS Fixture Proxy, interval=300, tolerance=50

[Rule]
FINAL,DNS Auto
