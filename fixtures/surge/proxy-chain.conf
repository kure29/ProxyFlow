[General]

[Proxy]
Entry Proxy = http, entry.example.com, 8080
Exit Proxy = http, exit.example.com, 8081

[Proxy Group]
Entry = select, Entry Proxy
Exit = select, Exit Proxy
Chain = select, Exit Proxy, underlying-proxy=Entry

[Rule]
DOMAIN-SUFFIX,example.com,Chain
FINAL,Chain
